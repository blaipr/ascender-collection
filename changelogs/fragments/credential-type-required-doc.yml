bugfixes:
  - credentials role - Documented ``credential_type`` as required. The variable table and the argument
    spec both said no, while the role applies ``| mandatory`` to it and the ``credential`` module declares
    it ``required=True``, so leaving it out has always failed.

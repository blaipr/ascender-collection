minor_changes:
  - application - Documented ``client_id`` and ``client_secret`` in ``RETURN``. The module has returned
    both since it started calling ``create_or_update_if_needed`` with ``auto_exit=False``, but neither
    appeared in the documentation, so the one chance to capture a confidential application's secret was
    not discoverable from the module docs.
